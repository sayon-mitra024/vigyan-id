import VLoader from "@/components/ui/VLoader";

export default function Loading() {
  return <main className="flex min-h-screen items-center justify-center bg-[#FFF8F6]"><VLoader size={96} label="Loading VIGYAN.ID" /></main>;
}
